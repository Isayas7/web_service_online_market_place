import { createContext } from "react";
const TogglePovider = createContext();
export const StoreManagerPageProvider = createContext();
export const StorePageProvider = createContext()

export default TogglePovider;
